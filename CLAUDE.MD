## 通用语言规范

用中文回答问题，注释也要以中文为主

## 文件翻译规范

将项目中的 .md 文件翻译成中文时，遵循以下规则：

1. **Frontmatter 元数据不翻译**：保留 `name`、`description`、`origin` 等元数据不变
2. **正文内容翻译**：将标题、段落、列表等翻译成中文
3. **代码块保留**：代码示例中的注释可保留英文或适当翻译
4. **中文标点**：使用中文标点符号（，。：；？！""）
5. **标题翻译**：文件标题翻译为中文（如 "Build and Fix" → "构建修复"）
